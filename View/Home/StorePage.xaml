<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LocalLink.ViewModel.HomeViewModel"
             xmlns:components="clr-namespace:LocalLink.View.Components"
             x:Class="LocalLink.View.Home.StorePage"
             Title="Mi Tienda">

    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="15,0" Spacing="0">

                <VerticalStackLayout Spacing="10" Margin="0,15,0,0">
                    <Border Stroke="{StaticResource Primary}"
                            StrokeThickness="2"
                            Padding="20"
                            BackgroundColor="{AppThemeBinding Light=White, Dark=#252525}">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15" />
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToAddProductCommand}" />
                        </Border.GestureRecognizers>

                        <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                            <Label Text="+" FontSize="40" HorizontalOptions="Center" TextColor="{StaticResource Primary}"/>
                            <Label Text="Publicar un nuevo producto" 
                                   FontAttributes="Bold" 
                                   TextColor="{StaticResource Primary}"/>
                        </VerticalStackLayout>
                    </Border>

                    <Label Text="Mis Productos" FontSize="20" FontAttributes="Bold" Margin="0,15,0,5" />
                </VerticalStackLayout>

                <CollectionView ItemsSource="{Binding Products}"
                                SelectionMode="None"
                                Margin="0,0,0,20">

                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <components:ProductItemView />
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Margin="0,40">
                            <Label Text="Aún no tienes productos publicados." 
                                   TextColor="Gray"
                                   HorizontalOptions="Center" />
                            <Label Text="¡Comienza a vender!" 
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center" />
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>

        <ActivityIndicator IsRunning="{Binding IsBusy}" 
                           IsVisible="{Binding IsBusy}"
                           VerticalOptions="Center" 
                           HorizontalOptions="Center"
                           Color="{StaticResource Primary}" />
    </Grid>
</ContentPage>