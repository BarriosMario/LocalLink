<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="LocalLink.View.Login.ChangePassword"
             NavigationPage.HasNavigationBar="False"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             Shell.FlyoutBehavior="Disabled"
             Shell.FlyoutItemIsVisible="False">

    <ContentPage.Resources>
        <ResourceDictionary>
            <toolkit:IsStringNotNullOrEmptyConverter x:Key="StringNotEmptyToBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid VerticalOptions="Fill" HorizontalOptions="Fill">
        <ScrollView VerticalOptions="Center">
            <VerticalStackLayout Spacing="25" Padding="30">

                <Label Text="Nueva Contraseña" 
                   FontSize="24" 
                   HorizontalOptions="Center" 
                   FontAttributes="Bold" />

                <Label Text="{Binding TextChangePassword}"
                   HorizontalTextAlignment="Center"
                   TextColor="Gray" />

                <VerticalStackLayout Spacing="15" Margin="0,10">
                    <Entry Placeholder="Contraseña" Text="{Binding NewPassword, UpdateSourceEventName=TextChanged}" IsPassword="True" />
                    <Label Text="{Binding PasswordError}" 
                           TextColor="Red" 
                           FontSize="12"
                           IsVisible="{Binding PasswordError, Converter={StaticResource StringNotEmptyToBoolConverter}}" />

                    <Entry Placeholder="Confirmar contraseña" Text="{Binding ConfirmPassword, UpdateSourceEventName=TextChanged}" IsPassword="True" />
                    <Label Text="{Binding ConfirmPasswordError}" 
                           TextColor="Red" 
                           FontSize="12"
                           IsVisible="{Binding ConfirmPasswordError, Converter={StaticResource StringNotEmptyToBoolConverter}}" />
                </VerticalStackLayout>

                <Button Text="Cambiar Contraseña"
                        Command="{Binding ChangePasswordCommand}"
                        HeightRequest="50"
                        CornerRadius="10"
                        FontAttributes="Bold">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroupList>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="#2196F3" />
                                        <Setter Property="TextColor" Value="White" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="#D3D3D3" />
                                        <Setter Property="TextColor" Value="#A9A9A9" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateGroupList>
                    </VisualStateManager.VisualStateGroups>
                </Button>

                <Button Text="Cancelar"
                    Command="{Binding CancelCommand}"
                    BackgroundColor="Transparent"
                    TextColor="Gray" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>